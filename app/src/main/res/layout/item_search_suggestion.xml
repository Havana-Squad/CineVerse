<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <variable
            name="item"
            type="com.karrar.movieapp.ui.search.mediaSearchUIState.SearchSuggestionUiState" />

        <variable
            name="listener"
            type="com.karrar.movieapp.ui.search.adapters.SearchSuggestionsInteractionListener" />

    </data>
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingVertical="14dp"
        android:paddingHorizontal="@dimen/spacing_medium">

        <TextView
            android:id="@+id/text_search_history"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            tools:text="Star Wars"
            android:text="@{item.name}"
            style="@style/Body_MD.Medium"
            android:textColor="@color/shade_secondary"
            android:drawablePadding="@dimen/spacing_small"
            app:drawableStartCompat="@drawable/ic_search"
            app:drawableTint="@color/shade_tertiary"
            android:gravity="center_vertical"
            android:onClick="@{() -> listener.onClickSearchSuggestion(item.name)}"/>
        <ImageView
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:src="@drawable/ic_arrow_right_up"
            app:tint="@color/shade_secondary" />
    </LinearLayout>

</layout>